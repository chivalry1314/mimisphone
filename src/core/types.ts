/**
 * 类型定义导出
 * @deprecated 请使用 @mimisOS/sdk 中的类型定义
 */

// 从 SDK 重新导出类型，保持向后兼容
export type {
  AppManifest,
  AppContext,
  AppProps,
  SystemPermission,
  BatteryInfo,
  NotificationOptions,
  GlobalSettings,
  WorldInfoEntry,
} from './sdk/types';
